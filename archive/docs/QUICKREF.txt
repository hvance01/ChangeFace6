┌─────────────────────────────────────────────────────────────┐
│           ChangeFace3 部署快速参考卡片                      │
└─────────────────────────────────────────────────────────────┘

🚀 一键部署（推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
git clone <仓库地址> /home/admin/app
cd /home/admin/app
bash deploy.sh

💡 核心命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
首次部署:     bash deploy.sh
快速重启:     bash restart.sh
打包应用:     bash package.sh
停止应用:     kill -9 $(lsof -ti:8501)

📦 systemd 服务（生产环境）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
安装服务:     sudo bash install-service.sh
启动服务:     sudo systemctl start changeface
停止服务:     sudo systemctl stop changeface
重启服务:     sudo systemctl restart changeface
查看状态:     sudo systemctl status changeface
查看日志:     sudo journalctl -u changeface -f

🔌 端口配置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
端口转发:     sudo bash setup-port-forward.sh
使用80端口:   export PORT=80 && sudo bash deploy.sh
自定义端口:   export PORT=3000 && bash deploy.sh

📋 常用检查命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
查看端口:     lsof -i:8501
查看进程:     ps aux | grep streamlit
测试连接:     curl localhost:8501
查看IP:       hostname -I

🔧 环境变量
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
export APP_DIR="/home/admin/app"
export GIT_REPO="https://github.com/你的用户名/repo.git"
export GIT_BRANCH="main"
export PORT=8501
export REPLICATE_API_TOKEN="r8_xxxxx"

📚 详细文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCRIPTS.md       - 脚本使用说明
DEPLOY_GUIDE.md  - 完整部署指南
PORT_CONFIG.md   - 端口配置详解

🆘 故障排查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
问题: 虚拟环境创建失败
解决: sudo apt install -y python3-venv python3-dev

问题: 端口被占用
解决: kill -9 $(lsof -ti:8501)

问题: 依赖安装失败
解决: source venv/bin/activate && pip install -r requirements.txt -v

问题: 无法访问应用
检查:
  1. 应用是否运行: lsof -i:8501
  2. 防火墙规则: sudo iptables -L -n
  3. 阿里云安全组是否开放端口

┌─────────────────────────────────────────────────────────────┐
│ 访问地址: http://服务器IP:8501                              │
│ 如配置端口转发: http://服务器IP                             │
└─────────────────────────────────────────────────────────────┘
